%% Gradient of the loss function
function res=gradloss(theta,X,Y)
  order = columns(theta)-1;

  ## ################################################################
  ## Your code in here!!!

  res = rand(size(theta)); ## dummy result
endfunction;
